{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "TableProps",
  "type": "object",
  "properties": {
    "columns": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "data": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": {
          "type": ["string", "number", "boolean", "object", "array", "null"]
        }
      }
    },
    "className": {
      "type": "string"
    },
    "renderCell": {
      "description": "Optional render callback, not directly representable in JSON schema"
    },
    "borders": {
      "type": "string",
      "enum": ["none", "rows", "cells"],
      "description": "Border style for the table: 'none' (no borders), 'rows' (horizontal row borders), 'cells' (borders around each cell)"
    }
  },
  "required": ["columns", "data"],
  "additionalProperties": false
}
